# ASR Benchmarking Pipeline — Dependencies
# Python >= 3.10

# Audio I/O
soundfile>=0.12.1
scipy>=1.11.0
numpy>=1.26.0

# ASR metrics
jiwer>=3.0.3

# HTTP clients
httpx>=0.27.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Environment config
python-dotenv>=1.0.0

# Optional: Faster-Whisper (comment out if not using GPU/local inference)
# faster-whisper>=1.0.3
# ctranslate2>=4.3.1

# Optional: Deepgram SDK (we use raw httpx above, but SDK available)
# deepgram-sdk>=3.5.0

# Dev
pytest>=7.4.0
